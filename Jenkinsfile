pipeline {

    agent any

    stages {

        stage('Setup') {
            echo "Setup Stag"
        }

        stage('Build') {
            echo "Build Stag"
        }

        stage('Release') {
            echo "Release Stag"
        }

        stage('Publication') {
            echo "Publication Stag"
        }
    }
}